# I-09 — não-goals respeitados

Nenhum comportamento foi condicionado a `service_type`. Não há fallback de política, BFF, command/query layer, autorização central, outbox unificada, máquina de estados nova ou redesign.
