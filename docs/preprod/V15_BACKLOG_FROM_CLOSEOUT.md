# Backlog V1.5 transferido do fechamento V1

Itens de MELHORIA que não entram no fechamento atual:

- tornar `operation_scope` obrigatório após remoção/isolamento das fixtures legadas;
- revisar a indivisibilidade do recurso físico estúdio quando Sabrina e Fê operarem em paralelo;
- consolidar colunas achatadas de `service_change_policies` e os snapshots `CHANGE_POLICY_SNAPSHOT_V1` / `CONSOLIDATED_POLICY_V2`;
- avaliar suporte futuro a sinal fixo se o desvio de centavos do percentual deixar de ser aceitável;
- observabilidade técnica, autorização central, outbox/idempotência unificada e demais não-goals explicitamente transferidos para V1.5/V2 pelo documento de fechamento.

Nenhum desses itens é implementado nesta fase.
