name: Retired Mercado Pago Staging Browser Gate

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Parallel staging retired
        run: |
          echo "This workflow is intentionally retired."
          echo "BlackSheep Agenda uses only the official operational environment."
          echo "Provider sandbox validation remains isolated to Mercado Pago provider-only gates."
